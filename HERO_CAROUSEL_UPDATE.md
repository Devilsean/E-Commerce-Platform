# 首页轮播图更新说明

## 更新内容

已成功实现首页轮播图的新布局：**左边显示描述性文字，右边显示随机商品**

## 主要改动

### 1. JavaScript 逻辑更新 (public/app.js)

#### 新增功能：
- **`loadHeroSlides()`** - 动态加载轮播图内容
  - 从后端获取商品列表
  - 随机选择3个商品展示在轮播图中
  - 每个轮播图包含：描述文字 + 随机商品卡片
  - 支持点击商品卡片跳转到商品详情页

- **`loadDefaultHeroSlides()`** - 加载失败时的降级方案
  - 显示默认的欢迎轮播图

#### 轮播图结构：
每个轮播图包含三个主题：
1. **欢迎来到精品商城** - 紫色渐变背景
2. **精选好物推荐** - 粉红色渐变背景  
3. **限时特惠** - 蓝色渐变背景

### 2. CSS 样式更新 (public/css/hero.css)

#### 新增样式类：
- `.hero-subtitle` - 副标题样式
- `.hero-description` - 描述文字样式
- `.hero-product` - 商品展示区域
- `.hero-product-card` - 商品卡片容器
  - 白色半透明背景
  - 圆角设计
  - 悬停动画效果
- `.hero-product-badge` - "推荐商品"徽章
- `.hero-product-image` - 商品图片区域
- `.hero-product-info` - 商品信息区域
- `.hero-product-price` - 商品价格显示
- `.hero-product-sales` - 销量显示

#### 样式特点：
- 响应式设计
- 流畅的过渡动画
- 现代化的渐变背景
- 卡片悬停效果（上浮 + 放大）
- 图片缩放效果

## 功能特性

### ✨ 核心特性
1. **动态商品展示** - 每次刷新页面随机展示3个不同商品
2. **左右布局** - 左侧描述文字，右侧商品卡片
3. **自动轮播** - 每5秒自动切换
4. **手动控制** - 支持左右箭头和底部指示点切换
5. **点击跳转** - 点击商品卡片直接进入商品详情页

### 🎨 视觉效果
- 三种不同的渐变背景色
- 商品卡片带阴影和毛玻璃效果
- 悬停时卡片上浮和图片放大
- "推荐商品"徽章突出显示

### 📱 用户体验
- 流畅的切换动画
- 清晰的商品信息展示（名称、描述、价格、销量）
- 直观的操作按钮
- 响应式布局适配

## 技术实现

### 数据流程
```
1. 页面加载 → renderHome()
2. 调用 loadHeroSlides()
3. 请求 /guest/products 获取商品列表
4. 随机选择3个商品
5. 生成轮播图HTML
6. 启动自动轮播
```

### 轮播图结构
```html
<div class="hero-banner">
  <div class="hero-slider">
    <div class="hero-slide">
      <div class="hero-content">
        <!-- 左侧：标题、副标题、描述、按钮 -->
      </div>
      <div class="hero-product">
        <div class="hero-product-card">
          <!-- 右侧：商品卡片 -->
        </div>
      </div>
    </div>
  </div>
  <div class="hero-dots"><!-- 指示点 --></div>
  <button class="hero-arrow"><!-- 左右箭头 --></button>
</div>
```

## 兼容性

- ✅ 支持现代浏览器
- ✅ 响应式设计
- ✅ 降级处理（商品加载失败时显示默认图标）
- ✅ 图片加载失败时显示占位图标

## 使用说明

无需额外配置，页面加载时自动运行。轮播图会：
1. 自动从后端获取商品数据
2. 随机选择商品展示
3. 自动开始轮播
4. 用户可手动控制切换

## 注意事项

- 需要后端 `/guest/products` 接口正常运行
- 商品需要包含 `image_url` 或 `main_image` 字段以显示图片
- 建议商品描述不要过长（会自动截断显示2行）